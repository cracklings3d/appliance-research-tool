# Contributing

## Git Workflow

- **Merge strategy**: Always rebase. Never merge with a merge commit.
- Branch names: `feature/`, `fix/`, `refactor/` prefixes
- Commits should be atomic and descriptive

## Stack

- Electron + Vue 3 + Vite
- See `docs/adr/` for architectural decisions
